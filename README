This repo contains a MATLAB app adapted from a provided flocking algorithm which takes certain parameters and generates flocking behaviour.

All of the other files are information/data used for testing purposes.
